// This file is now only for static config/settings.
// Initial data (resources, etc.) has been moved to seed.ts.

export const settings = {
    defaultDuration: 240, // 4 hours
    startTime: '08:00',
    endTime: '22:00',
    closedDays: [0], // Sunday
    weekStartsOn: 1,
};
